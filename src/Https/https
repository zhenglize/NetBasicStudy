HTTPS
定义
HTTP over SSL 的简称，即工作在 SSL （或 TLS）上的 HTTP。说白了就是加密通信的 HTTP。
工作原理
在客户端和服务器之间协商出一套对称密钥，每次发送信息之前将内容加密，收到之后解密，达到内容的加密传输

为什么不直接用非对称加密？
非对称加密由于使用了复杂了数学原理，因此计算相当复杂，如果完全使用非对称加密来加密通信内
容，会严重影响网络通信的性能

Https通信流程
客户端请求建立TLS连接
服务器发回证书
客户端验证服务器证书
客户端信任服务器后，和服务器协商对称密钥
使用对称密钥开始通信