status Code 状态码
三位数字，⽤用于对响应结果做出类型化描述（如「获取成功」「内容未找到」）

1xx：临时性消息。
如：100 （继续发送，通常是传输大文件一次不能传完，服务器返回100意思是继续传）
101（正在切换协议.比如说从HTTP1.1切换到HTTP2.0，切换之前发送状态码进行通知）

2xx：成功。
最典型的是 200（OK）、201（创建成功）

3xx：重定向。
如 301（永久移动）、302（暂时移动）、304（内容未改变）

4xx：客户端错误。
如 400（客户端请求错误）、401（认证失败）、403（被禁止）、404（找不到内容）。

5xx：服务器错误。
如 500（服务器内部错误）。